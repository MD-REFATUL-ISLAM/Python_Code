

# Keyword Arguments : The idea is to allow the caller
# to specify the argument name with values so that
# the caller does not need to remember the order of parameters.


def student(firstname,lastname):
    print(firstname,lastname)

student(lastname = "ashrafie", firstname="Salasabil")
student(lastname = "islam",firstname = "refatul")
